{
  "name": "grt-token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ socr102/grt-token",
    "create-local": "graph create --node http://localhost:8020/ socr102/grt-token",
    "remove-local": "graph remove --node http://localhost:8020/ socr102/grt-token",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 socr102/grt-token"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
